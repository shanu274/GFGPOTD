class Solution {
  public:
    int minCandy(vector<int> &arr) {
        // initialize
        int n = arr.size();
        vector<int> candy(n, 1);
        
        //left to right
        for(int i = 1; i < n; i++){
            if(arr[i] > arr[i-1]){
                candy[i] = candy[i-1]+1;
            }
        }
        //right to left
        for(int i = n-2; i >= 0; i--){
            if(arr[i] > arr[i+1]){
                candy[i] = max(candy[i], candy[i+1]+1);
            }
        }
        
        //sum
        int total = 0;
        for(int c : candy)
            total += c;
        
        return total;
        
    }
};
